pipeline {
  agent {
    // this image provides everything needed to run Cypress
   
  }

  stages {
    stage('build and test') {

      steps {
        sh 'npm ci'
        sh "npm run test:ci:record"
      }
    }
  }
}
